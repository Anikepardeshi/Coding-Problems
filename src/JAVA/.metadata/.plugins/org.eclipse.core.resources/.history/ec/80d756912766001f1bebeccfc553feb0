package bank;

//3. SavingsAccount.java (Inheritance)
//Purpose: 
//- SavingsAccount is a subclass of BankAccount that adds functionality for interest calculation. 
//- It inherits all the properties and methods of BankAccount and introduces an interestRate attribute.
//Inheritance: 
//- The SavingsAccount class inherits from BankAccount, meaning it gets all the properties 
//  and behaviors of a regular bank account (like deposit, withdraw). 
//- It also overrides the calculateInterest() method to apply interest specific to savings accounts.

public class SavingsAccount extends BankAccount{
	
	private double interestRate;
	
	public SavingsAccount(String accountNumber, double initialBalance,AccountHolder accountHolder,double interestRate) {
		super(accountNumber,initialBalance,accountHolder);
		this.interestRate = interestRate;
	}
	
	@Override
	public void calculateInterest() {
		double interest = getBalance()*interestRate/100;
		deposit(interest);
		transactions.add(new Transaction("Interest", interest, getBalance()));
	}
}
